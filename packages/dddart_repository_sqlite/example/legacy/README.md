# Legacy SQLite collection sketch

`collection_examples.dart.skip` is retained for historical context. It uses
obsolete positional entity constructors, value equality, and connection APIs
and is not a runnable example of the current SQLite repository contract.

Use the runnable examples in the parent directory instead.

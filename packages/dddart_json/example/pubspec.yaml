name: dddart_json_example
description: Example demonstrating DDDart JSON serialization features
version: 0.9.0
resolution: workspace

environment:
  sdk: ^3.9.0

dependencies:
  dddart:
    resolution: workspace
  dddart_serialization:
    resolution: workspace
  dddart_json:
    resolution: workspace

dev_dependencies:
  build_runner: ^2.15.0
  test: ^1.24.0

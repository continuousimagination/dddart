# Test configuration for dddart_events_distributed

tags:
  # Opt-in smoke tests that require real AWS AppSync Events resources.
  aws:
    timeout: 3x

  # Property-based tests
  property-test:
    timeout: 2x

  # Generator tests that use build_test
  generator:
    timeout: 3x

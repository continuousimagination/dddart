/// DynamoDB repository exception utilities.
///
/// This file will contain exception mapping logic in a later task.
/// For now, it's a placeholder to allow the package to be analyzed.
class DynamoRepositoryException {
  /// Creates a [DynamoRepositoryException].
  const DynamoRepositoryException();
}

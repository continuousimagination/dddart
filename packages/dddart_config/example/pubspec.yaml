name: dddart_config_example
description: Examples demonstrating DDDart configuration management
version: 0.1.0
resolution: workspace

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  dddart_config:
    resolution: workspace

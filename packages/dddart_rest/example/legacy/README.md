# Illustrative legacy authentication sketch

`self_hosted_auth_mongodb_example.dart.skip` is a conceptual MongoDB
authentication sketch. It uses obsolete constructors and does not represent a
runnable generated repository boundary. The active in-memory authentication
example remains in the parent directory.

# Test configuration for dddart_repository_mysql

# Run all tests serially to avoid MySQL database conflicts
concurrency: 1

tags:
  # Tests that require a running MySQL instance
  requires-mysql:
    timeout: 2x
  
  # Property-based tests
  property-test:
    timeout: 2x
  
  # Integration tests
  integration:
    timeout: 2x

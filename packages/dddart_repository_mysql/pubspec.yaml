name: dddart_repository_mysql
description: MySQL database repository implementation for DDDart aggregate roots with automatic code generation support.
version: 0.9.0
repository: https://github.com/yourusername/dddart
resolution: workspace

environment:
  sdk: ^3.9.0

dependencies:
  analyzer: ^8.4.1
  build: ^4.0.0
  dddart:
    resolution: workspace
  dddart_repository_sql:
    resolution: workspace
  dddart_serialization:
    resolution: workspace
  mysql_client: ^0.0.27
  source_gen: ^4.2.3

dev_dependencies:
  build_runner: ^2.15.0
  build_test: ^3.3.2
  dddart_json:
    resolution: workspace
  test: ^1.24.0
  very_good_analysis: ^6.0.0

# Legacy MySQL collection sketch

`collection_examples.dart.skip` is retained for historical context. Collection
mapping in this sketch is not supported by the current generator contract, so
the file is deliberately excluded from generation and execution.

Use the runnable examples in the parent directory instead.
